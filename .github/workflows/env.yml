name: Create envfile

on: [ push ]

jobs:

  create-envfile:
 
    runs-on: ubuntu-latest
 
    steps:
    - name: Make envfile
    run: |
          touch .env
          echo DJANGO_KEY= ${{ secrets.DJANGO_KEY }} >> .env
          cat .env
      
